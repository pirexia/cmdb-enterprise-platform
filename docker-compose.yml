version: "3.9"

# ──────────────────────────────────────────────────────────────────────────────
# CMDB Enterprise Platform — Full-stack Docker Compose
#
# Tested on: RHEL 8/9, CentOS Stream 9, Ubuntu 22.04, macOS (Docker Desktop)
#
# Quick start:
#   cp .env.example .env        # configure variables
#   docker compose up -d --build
#
# SELinux (RHEL/CentOS): The :Z suffix on volume mounts tells the container
# engine to relabel the volume content so SELinux Enforcing mode allows access.
# ──────────────────────────────────────────────────────────────────────────────

services:

  # ── PostgreSQL 15 ────────────────────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    container_name: cmdb-postgres
    restart: always
    environment:
      POSTGRES_DB:       ${POSTGRES_DB:-cmdb_db}
      POSTGRES_USER:     ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-cmdb_pass123}
    volumes:
      # :Z relabels the volume for SELinux Enforcing mode (RHEL/CentOS)
      - postgres-data:/var/lib/postgresql/data:Z
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-admin} -d ${POSTGRES_DB:-cmdb_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ── Adminer — lightweight DB management UI ───────────────────────────────────
  adminer:
    image: adminer:latest
    container_name: cmdb-adminer
    restart: always
    ports:
      - "${ADMINER_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy

  # ── Backend — Express + Prisma API ───────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cmdb-backend
    restart: always
    environment:
      NODE_ENV:     production
      PORT:         3000
      DATABASE_URL: postgresql://${POSTGRES_USER:-admin}:${POSTGRES_PASSWORD:-cmdb_pass123}@postgres:5432/${POSTGRES_DB:-cmdb_db}
      JWT_SECRET:   ${JWT_SECRET:?JWT_SECRET is required — set it in .env}
    ports:
      - "${BACKEND_PORT:-3000}:3000"
    depends_on:
      postgres:
        condition: service_healthy

  # ── Frontend — Next.js standalone ────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Baked into the client-side JS bundle at build time.
        # Must point to the backend as reachable from the BROWSER
        # (i.e. the server's public IP or domain name, not "backend")
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3000}
    container_name: cmdb-frontend
    restart: always
    environment:
      NODE_ENV: production
      PORT:     3001
    ports:
      - "${FRONTEND_PORT:-3001}:3001"
    depends_on:
      - backend

# ── Volumes ───────────────────────────────────────────────────────────────────
volumes:
  postgres-data:
    name: cmdb-postgres-data

# ── Networks ──────────────────────────────────────────────────────────────────
networks:
  default:
    name: cmdb-network
